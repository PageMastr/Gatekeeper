# CONTEXT — {{NAME}}

> Decisions that are **settled**. This file is the reason a fresh session does
> not re-litigate the game every time it wakes up.
>
> Rule: a decision enters this file with its *reason*. A decision without a
> reason gets re-opened by the next agent that disagrees with it, and you pay
> for the argument twice.

## The game in three lines

- **Genre / shape:** <…>
- **Feeling we are chasing:** <…>
- **Reference:** <path or URL; see COLOR_BIBLE.md for the translation>

## The reference, specifically

What is being taken from it, and what is not. "Atmospheric" is not a reference;
"the light comes from one source and everything else falls off into black" is.

- **What matters about it:** <…>
- **What we are explicitly NOT taking:** <…>
- **The lighting condition of the game** (which is usually not the reference's):
  <time of day, weather, light sources>

## Settled decisions

| # | decision | reason | reversible? | date |
|---|---|---|---|---|
| 1 | Godot 4 Forward+ renderer | mobile/compat not a target; we want real shadows and volumetric fog | no (one-way door) | {{DATE}} |
| 2 | Blender assets are generated by Python scripts, never hand-modelled | a script can be re-run, reviewed and diffed; a mesh edit cannot | no | {{DATE}} |
| 3 | Assets ship as .glb, not .blend | no Blender dependency at import time; the .glb is a build artifact | yes | {{DATE}} |

## Systems

The behaviours that make the world move. The roadmap's systems inventory says
*when* each is built; this says *what it is*. A system the player cannot read is
a system that does not exist to them.

| system | state it holds | what changes it | what the player sees of it |
|---|---|---|---|
| <…> | <one number / flags / a grid / a texture> | <the player, time, another system> | <the readout, the sound, the light> |

**Interactions worth writing down** — systems that move each other, because
tuning one moves the other and they have to be greyboxed together:

- <system A raises system B, so B cannot be tuned before A exists>

## The world

The shape of the place. Sizes in metres, decided here rather than discovered
during a build. The roadmap's levels table says which stage blocks each one out.

```
<ASCII map sketch: spaces, connections, and where the player enters>
```

| space | size (m) | what happens here | connects to |
|---|---|---|---|
| <…> | <…> | <…> | <…> |

- **Fixed or generated:** <…; if generated, the rules and what must always hold>
- **Where the pressure comes from, spatially:** <…>
- **Where the player is safe:** <…>

## Session shape

- **One sitting is:** <…minutes>
- **Player starts with:** <…>
- **Player ends with:** <…>
- **Progression across sessions:** <none / what carries>

## Technical shape

- **Locomotion:** <CharacterBody3D / custom / physics>
- **Animation approach:** <rigged clips in Blender / mocap / procedural — pick one and say why>
- **World streaming:** <single scene / chunked around the player / none>
- **Save model:** <none / snapshot / event log>
- **Accumulating world state:** <e.g. footprints and damage render into one fixed-size texture, not a list of instances>

## One-way doors

Choices that are expensive to reverse. Each gets a checkpoint in the plan before
it is taken — never taken inside a parallel wave.

| door | taken? | date | notes |
|---|---|---|---|
| Renderer choice | yes | {{DATE}} | Forward+ |
| Perspective / camera | no | | |
| Animation approach | no | | |
| Single scene vs streamed world | no | | |
| Coordinate scale (1 unit = 1 m) | yes | {{DATE}} | assert in every asset spec |

## Vocabulary

Shared names, so two agents building adjacent systems do not invent two words
for the same thing.

| term | means |
|---|---|
